<article class="post">
    <form class="comment-form post m-2 p-2">
        <div class="form-group title textFont">
            <h4 href="{{post_url}}" target="_blank" class="text-body font-weight-bold"><a href="/post/{{id}}" target="_blank" class="textFont text-body">{{title}}</a></h4>
            <h5><p class="card-text text-muted">{{format_url post_url}}</p></h5>
            <p class="text-info">Posted by {{user.username}} on {{format_date created_at}}</p>
        </div>
    </form>
</article>